#pragma once

// Window Size
const int WINDOW_WIDTH = 1600;
const int WINDOW_HEIGHT = 900;

// Sidebar Dimensions
const int LEFT_BAR_WIDTH = 320;  // 20% of width
const int RIGHT_BAR_WIDTH = 320; // 20% of width
const int GRID_WIDTH = 960;     // 60% of width

// Grid and Cell Size
const int GRID_SIZE = 12;              // 12x12 grid
const float CELL_SIZE = GRID_WIDTH / GRID_SIZE; // Dynamically calculates cell size
